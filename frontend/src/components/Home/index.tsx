import React from 'react';
import { Container, Flex, Title } from '@mantine/core';

const Home = () => (
  <Container fluid>
    <Flex direction="row" wrap="wrap">
      <Title order={6}>home</Title>
    </Flex>
  </Container>
);

export default Home;
